import { AuthLayouts } from "@/common/layout";
import RecapReading from "@/modules/recapReadingData";

export default function RecapReadingPage() {
  return (
    <AuthLayouts>
      <RecapReading />
    </AuthLayouts>
  );
}
